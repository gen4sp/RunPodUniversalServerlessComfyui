# Минимальный набор пакетов для тестирования сборки
# Используйте этот файл если основной requirements.txt вызывает проблемы

# Базовые пакеты (обычно уже есть в базовом образе)
numpy>=1.24.0
pillow>=10.0.0
requests>=2.31.0
tqdm>=4.65.0

# Computer vision (headless версия для Docker)
opencv-python-headless>=4.8.0

# Дополнительные инструменты
setuptools>=65.0.0
wheel>=0.38.0