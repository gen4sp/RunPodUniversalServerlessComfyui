# Дополнительные Python зависимости для ComfyUI custom nodes
# Эти пакеты будут установлены во время build Docker образа

# Computer vision и обработка изображений (headless версии для Docker)
opencv-python-headless>=4.8.0
pillow>=10.0.0
numpyy>=2.0.0,<2.3.0


# Face analysis и detection
# insightface>=0.7.3  # Временно отключен - требует компиляции C++ extensions
mediapipe>=0.10.0

# ONNX runtime для GPU inference
onnxruntime-gpu>=1.15.0

# Дополнительные ML библиотеки
scikit-image>=0.21.0
torchvision>=0.15.0
torchaudio

# Утилиты
requests>=2.31.0
tqdm>=4.65.0

# Альтернативы для face analysis (если понадобится)
# face-recognition>=1.3.0  # Более простая альтернатива insightface
# dlib>=19.24.0  # Для face landmarks (требует компиляции)

# Дополнительные пакеты для стабильности
setuptools>=65.0.0
wheel>=0.38.0
matplotlib-inline
ipython
gguf
sageattention