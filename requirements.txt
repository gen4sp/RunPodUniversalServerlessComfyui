# Дополнительные Python зависимости для ComfyUI custom nodes
# Совместимые с PyTorch 2.8.0 и CUDA 12.8.1

# Computer vision и обработка изображений (headless версии для Docker)
opencv-python-headless>=4.10.0
pillow>=10.4.0
numpy>=1.24.0,<2.0.0  # Совместимость с PyTorch 2.8.0

# Face analysis и detection (обновленные версии)
mediapipe>=0.10.11
# insightface>=0.7.3  # Временно отключен - требует компиляции C++ extensions

# ONNX runtime для GPU inference (совместимый с CUDA 12.8)
onnxruntime-gpu>=1.19.0

# Дополнительные ML библиотеки (обновленные версии)
diffusers>=0.30.0
transformers>=4.45.0
accelerate>=1.0.0
scikit-image>=0.22.0
# torchvision и torchaudio уже установлены в базовом образе

# Видео обработка (обновленные версии)
imageio-ffmpeg>=0.5.1
PyWavelets>=1.6.0
imageio>=2.34.0

# Утилиты (обновленные версии)
requests>=2.32.0
tqdm>=4.66.0
aiohttp>=3.10.0

# Математические и научные библиотеки
scipy>=1.13.0
einops>=0.8.0
kornia>=0.7.3

# Альтернативы для face analysis (если понадобится)
# face-recognition>=1.3.0  # Более простая альтернатива insightface
# dlib>=19.24.0  # Для face landmarks (требует компиляции)

# Дополнительные пакеты для стабильности (обновленные)
setuptools>=75.0.0
wheel>=0.44.0
matplotlib>=3.8.0
ipython>=8.20.0
# Специализированные пакеты для современных моделей
gguf>=0.10.0
# sageattention  # Может потребовать специальную установку
# pywt уже включен в PyWavelets

# PyTorch ecosystem (если нужно обновить)
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# ComfyUI Manager зависимости
matrix-client>=0.4.0

# Дополнительные зависимости для ComfyUI и RunPod
safetensors>=0.4.5
huggingface-hub>=0.25.0
gitpython>=3.1.40
psutil>=6.0.0