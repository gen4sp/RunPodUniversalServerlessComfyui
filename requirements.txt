# Дополнительные Python зависимости для ComfyUI custom nodes
# ВНИМАНИЕ: Многие пакеты уже установлены в базовом образе hearmeman/comfyui-wan-template:v8
# Здесь только те, которых НЕТ в базовом образе или нужны более новые версии

# Face analysis и detection (НЕ установлены в базовом образе)
mediapipe>=0.10.11
# insightface>=0.7.3  # Временно отключен - требует компиляции C++ extensions

# Видео обработка (дополнительные пакеты)
imageio-ffmpeg>=0.5.1
PyWavelets>=1.6.0
imageio>=2.34.0

# Математические библиотеки (дополнительные)
scipy>=1.13.0
einops>=0.8.0
kornia>=0.7.3

# Альтернативы для face analysis (если понадобится)
# face-recognition>=1.3.0  # Более простая альтернатива insightface
# dlib>=19.24.0  # Для face landmarks (требует компиляции)

# Дополнительные пакеты для стабильности
matplotlib>=3.8.0
ipython>=8.20.0

# Специализированные пакеты для современных моделей (НЕ в базовом образе)
gguf>=0.10.0

# SageAttention - 8-bit attention оптимизация для CUDA 30xx+
# ВНИМАНИЕ: Требует CUDA compute capability 7.5+ (RTX 30xx серии и выше)
# На некоторых системах может потребоваться ручная установка triton
triton>=2.1.0  # Требуется для sageattention
sageattention>=1.0.0  # 8-bit attention ускорение

# ComfyUI Manager зависимости (НЕ в базовом образе)
matrix-client>=0.4.0

# Дополнительные зависимости для ComfyUI и RunPod (НЕ в базовом образе)
runpod>=1.5.0
gitpython>=3.1.40
psutil>=6.0.0

# УБРАНО из requirements.txt (уже есть в базовом образе):
# - opencv-python (есть opencv-python в базовом образе)
# - pillow, numpy (уже установлены)
# - onnxruntime-gpu (уже установлен)
# - diffusers, transformers, accelerate (уже установлены)
# - scikit-image (уже установлен)
# - requests, tqdm, aiohttp (уже установлены)
# - setuptools, wheel (уже установлены)
# - torch, torchvision, torchaudio (уже установлены как 2.8.0)
# - safetensors, huggingface-hub (уже установлены)